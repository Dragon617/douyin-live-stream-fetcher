/**
 * 简化的登录方案 - 手动粘贴Cookie（最可靠）
 * 自动获取Cookie需要本地运行，且依赖Puppeteer
 * 
 * 推荐使用流程：
 * 1. 用户运行 `npm run login` - 自动打开浏览器并保存Cookie
 * 2. 或者：手动粘贴Cookie（适用于任何部署场景）
 */

// 这个文件保留作为备用方案说明
console.log('推荐使用：npm run login');
